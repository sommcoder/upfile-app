{% comment %} 
      * FileViewer is a container for the files that have been submitted by the user. 
      - the files will arrange in a row as they are submitted 
      - the component should be able to handle MULTIPLE file uploads at ONE time and ALSO in succession 
{% endcomment %}
<span class="fileviewer--wrapper" >
  <div class="fileviewer--item-list" data-primary="{{ block.settings.primary-color }}" data-secondary="{{ block.settings.secondary-color }}" data-font="{{ block.settings.font-color }}" data-progress="{{ block.settings.progress-color }}">
      <div class="fileviewer--placeholder">files will appear here</div> 
  </div>
</span>

{% schema %}
  {
    "name": "File Viewer",
    "target": "section",
    "javascript": "dropzone.js",
    "stylesheet": "fileviewer.css",
    "enabled_on": {
      "templates": ["product"]
    },
    "settings": [
      {
        "type": "color",
        "id": "primary-color",
        "label": "Primary File Colour",
        "default": "#3d3d3d"
      }, {
        "type": "color",
        "id": "secondary-color",
        "label": "Secondary File Colour",
        "default": "#535353"
      }, {
        "type": "color",
        "id": "font-color",
        "label": "Icon Font Colour",
        "default": "#FFFFFF"
      },
      {
        "type": "color",
        "id": "progress-bar-color",
        "label": "Progress Bar Color",
        "default": "#03f46f"
      }
    ]
  }
{% endschema %}