<span class="dropzone-wrapper" style="border: 1px dotted{{block.settings.default-border-color}};">
  <span class="dropzone-text">{{ block.settings.default-text }}</span>
</span>

{% schema %}
  {
    "name": "File Viewer",
    "class": "shopify-block",
    "target": "section",
    "enabled_on": {
      "templates": ["product"]
    },
    "stylesheet": "dropzone.css",
    "javascript": "dropzone.js",
    "settings": [
          {
        "type": "text",
        "id": "default-text",
        "label": "Default Text",
        "default": "Drag and Drop Files Here"
      },
      {
        "type": "color",
        "id": "default-border-colour",
        "label": "Default Border Colour",
        "default": "#201e1e"
      },
      {
        "type": "checkbox",
        "id": "dotted-border",
        "label": "Dotted Border",
        "default": true
      },
      {
        "type": "text",
        "id": "drag-error-message",
        "label": "Drag Error Message",
        "default": "Oops.. this isn't a valid file"
      }
    ]
  }
{% endschema %}